using Marten;

namespace MESNET.Internship.Persistence;

public static class MartenConfiguration
{
    public static void ConfigureInternshipSchema(this StoreOptions options)
    {
        // Existing entity configurations are already in place
        // This method placeholder is for future schema customizations
    }
}
